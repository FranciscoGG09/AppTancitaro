name: tancitaro
description: App móvil para reportes ciudadanos

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  camera: ^0.10.5+5
  image_picker: ^1.0.4
  geolocator: ^10.0.0
  uuid: ^3.0.7
  sqflite: ^2.3.0
  path_provider: ^2.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  intl: ^0.18.1
  flutter_dotenv: ^5.1.0
  email_validator: ^2.1.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  assets:
    - assets/logo_municipal.png
    - assets/terms_conditions.txt